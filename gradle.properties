org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
# The Kotlin compiler runs in its own daemon; the Compose compiler plugin's stability inference is the
# memory-hungry part of this build.
kotlin.daemon.jvmargs=-Xmx3g
android.useAndroidX=true
android.nonTransitiveRClass=true
