<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Only for the ongoing notice that a SIM is currently disguised. Nothing else this app does posts a
      notification, so a refusal costs the user the reminder and the shortcut back, never an operation.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <instrumentation
        android:name="com.riteldevelopment.carriertestoverride.CarrierConfigInstrumentation"
        android:functionalTest="true"
        android:handleProfiling="false"
        android:targetPackage="${applicationId}" />

    <!--
      Package visibility. Stopping and wiping happen as the shell UID, which sees everything, so none of
      this grants power — it exists so the screen can tell installed from missing, draw real icons, and
      offer a list to choose from instead of claiming every app is absent.

      The <intent> filter is what makes a user-chosen target list possible: it reveals every launchable
      app without QUERY_ALL_PACKAGES, which is a policy-restricted permission this tool has no business
      holding just to populate a picker.

      The named packages stay anyway. <intent> only matches apps with a LAUNCHER entry, and a default
      target that ships without one would otherwise vanish from its own list. Shizuku is named so the
      top-bar shortcut can resolve a launch intent and tell "not installed" apart from "no launcher".
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <package android:name="com.sec.android.app.samsungapps" />
        <package android:name="com.samsung.android.voc" />
        <package android:name="com.zhiliaoapp.musically" />
        <package android:name="moe.shizuku.privileged.api" />
    </queries>

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
          singleTop so the ongoing notification's Restore button reaches the running screen through
          onNewIntent, rather than tearing it down and building a second copy of it.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:process=":ui">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".InstrumentationHostService"
            android:exported="false" />

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:process=":ui"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!--
          The default process is a minimal host for NO_RESTART instrumentation. Startup initializers
          belong with the Compose UI, not in that timing-sensitive host process.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            android:process=":ui"
            tools:node="merge" />

    </application>

</manifest>
